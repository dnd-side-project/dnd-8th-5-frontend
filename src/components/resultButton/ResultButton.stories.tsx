import ResultButton from './ResultButton';

export default {
  title: '최종 결과/하단 버튼',
  component: ResultButton,
};

const Template = () => <ResultButton />;

export const Default = Template.bind({});
