import CurrentCalendar from './CurrentCalendar';

export default {
  title: '조율 현황/캘린더',
  component: CurrentCalendar,
};

const Template = () => <CurrentCalendar />;

export const Default = Template.bind({});
