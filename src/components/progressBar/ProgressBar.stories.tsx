import ProgressBar from './ProgressBar';

export default {
  title: '조율 현황/Progress Bar',
  component: ProgressBar,
};

const Template = () => <ProgressBar />;

export const Default = Template.bind({});
